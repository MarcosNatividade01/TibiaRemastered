# Test Room Map Patch

Small artificial patch used only to validate the Remastered Map Patch Pipeline.

It does not contain a real OTBM fragment and must not be promoted to runtime. The patch validates metadata, feature flag handling, monster spawn merge, teleport validation, sandbox backup, rollback, and reapplication behavior.
